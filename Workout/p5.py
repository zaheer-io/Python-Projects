# 5. Reverse Dictionary

# {"a": 1, "b": 2, "c": 1}
#
# Convert into a dictionary where keys are values and values are lists of keys:
#
# {1: ["a", "c"], 2: ["b"]}

# dicta = {"a": 1, "b": 2, "c": 1}
# dictb = {dicta.values() : [i for i in dicta.keys() if ]}